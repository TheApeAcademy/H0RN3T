{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npm install --legacy-peer-deps && npx turbo run build --filter=@h0rn3t/web",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install --legacy-peer-deps",
  "crons": [
    {
      "path": "/api/threat-feed/ingest",
      "schedule": "0 0 * * *"
    }
  ]
}
